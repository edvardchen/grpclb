import createGrpcProxy from './createGrpcProxy';

import createClientPool from './createClientPool';

import { initGlobalResolver } from './globalClientPool';

export { createGrpcProxy, createClientPool, initGlobalResolver };
