import createGrpcProxy from './createGrpcProxy';

import createClientPool from './createClientPool';

import { globalResolver } from './globalClientPool';

export { createGrpcProxy, createClientPool, globalResolver };
